/**
 * theme/index.ts — Barrel export for the design system
 * 
 * Import everything from '@/theme' for clean access to colors, spacing, typography.
 */

export { colors } from './colors';
export { spacing, borderRadius } from './spacing';
export { typography } from './typography';
